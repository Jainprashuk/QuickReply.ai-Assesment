import { Meta, Title, Description, Primary, Stories } from '@storybook/blocks';

<Meta title="Introduction" />

# Welcome to the Component Library

This library contains a collection of reusable React components built using Storybook. Each component is designed to be flexible and easy to integrate into your applications.

## Getting Started

To start using the components, simply import them into your project:

```jsx
import { NewSlider } from 'your-component-library';
